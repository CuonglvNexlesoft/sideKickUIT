// import Locale from '../utils/Locale';

export const APP = {
    settings: {},
    headerText: '',
    loading: false
}

export const SETTING = {
    settings: {},
}

export const USER = {
    user: null,
    rehydrate: false
}

export const PRODUCT = {
    products: [],
}